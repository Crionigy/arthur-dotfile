# Config for themes
include $HOME/.config/sway/definitions/theme.conf

# Variables used throughout the system
include $HOME/.config/sway/definitions/variables.conf

# Insert here the autostart file
include $HOME/.config/sway/autostart

# Variables and keybinds for modes
include $HOME/.config/sway/modes.d/*

# System configs
include $HOME/.config/sway/config.d/*

# waybar blur
layer_effects "waybar" blur enable; shadows disable;
layer_effects "gtk-layer-shell" blur enable; shadows enable; corner_radius 13
layer_effects "wofi" blur enable; shadows enable; corner_radius 13
layer_effects "avizo" blur enable; corner_radius 15

smart_corner_radius enable
corner_radius 15
shadows enable
shadows_on_csd enable
shadow_blur_radius 20

# blur
blur enable
blur_xray disable
blur_passes 4
blur_radius 4
